import { MatCardBackgroundColorDirective } from './mat-card-background-color.directive';

describe('MatCardBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatCardBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
