import { CheckboxColorDirective } from './checkbox-color.directive';

describe('CheckboxBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxColorDirective();
    expect(directive).toBeTruthy();
  });
});
