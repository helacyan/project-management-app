import { CursorMoveDirective } from './cursor-move.directive';

describe('CursorMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorMoveDirective();
    expect(directive).toBeTruthy();
  });
});
