import { MatCardTransformDirective } from './mat-card-transform.directive';

describe('MatCardTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new MatCardTransformDirective();
    expect(directive).toBeTruthy();
  });
});
