import { HighlightFooterLinkDirective } from './highlight-footer-link.directive';

describe('HighlightFooterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFooterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
